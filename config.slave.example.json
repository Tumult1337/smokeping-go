{
  "cluster": {
    "master_url": "https://master.example.internal",
    "token": "${CLUSTER_TOKEN}",
    "name": "frankfurt-1",
    "advertise": "${ADVERTISE_IP}",
    "push_every": "5s",
    "pull_every": "60s"
  }
}
