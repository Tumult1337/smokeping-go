/smokeping2gosmokeping
/gosmokeping
/config.json
ui/node_modules/
ui/dist/
# Local Vite overrides for verifying the UI against a live API (e.g.
# vite.config.live.ts). The checked-in vite.config.ts stays pinned at
# localhost; per-env overrides are local-only and never committed.
ui/vite.config.*.ts
internal/ui/dist/*
!internal/ui/dist/.gitkeep
*.log
.env
.DS_Store
.playwright-mcp/
.claude/
# Skills are shared project rules like CLAUDE.md; local settings are not.
!.claude/skills/
!.claude/skills/**
.worktrees/
todo.md
docs/superpowers/
# tsc -b rewrites this on every build; tracking it makes every UI build dirty the tree.
ui/*.tsbuildinfo
