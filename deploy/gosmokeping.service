[Unit]
Description=gosmokeping — modern smokeping replacement
Documentation=https://github.com/tumult/gosmokeping
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=gosmokeping
Group=gosmokeping
ExecStart=/opt/smokeping/gosmokeping -config /opt/smokeping/config.json
ExecReload=/bin/kill -HUP $MAINPID
Restart=on-failure
RestartSec=5s

# ICMP needs raw sockets. Granted here so the binary itself doesn't need setcap.
AmbientCapabilities=CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_RAW

# Hardening — standard systemd knobs.
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
PrivateDevices=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictAddressFamilies=AF_INET AF_INET6 AF_NETLINK
RestrictNamespaces=true
RestrictRealtime=true
LockPersonality=true
MemoryDenyWriteExecute=true
ReadWritePaths=/opt/smokeping/state

[Install]
WantedBy=multi-user.target
